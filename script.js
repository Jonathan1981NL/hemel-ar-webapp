
const DATA=window.SKY_EXPLORER_DATA, VERSION=DATA.version, EARTH=12742, SUN=1392700, DEFAULT_LAT=52.085, DEFAULT_LON=4.883;
let objects=[...DATA.bodies.filter(o=>o.id!=="earth"),...DATA.stars];
const state={heading:95,rawHeading:95,pitch:28,rawPitch:28,headingOffset:0,pitchOffset:0,demo:false,motion:false,started:false,selected:"sun",locked:null,tab:"story",filter:"visible",stars:[],lat:DEFAULT_LAT,lon:DEFAULT_LON,voyagerStep:0,voyagerRunning:false,voyagerTimer:null,voyagerRoute:"voyager"};
const $=id=>document.getElementById(id);
const el={start:$("start"),camera:$("camera"),starfield:$("starfield"),sky:$("skyLayer"),edge:$("edgeLayer"),guide:$("guideText"),toast:$("toast"),heading:$("headingChip"),motionChip:$("motionChip"),gps:$("gpsChip"),calChip:$("calChip"),inspector:$("inspector"),selectedSphere:$("selectedSphere"),referenceSphere:$("referenceSphere"),referenceLabel:$("referenceLabel"),selectedLabel:$("selectedLabel"),scaleMode:$("scaleMode"),objectGroup:$("objectGroup"),objectName:$("objectName"),objectSummary:$("objectSummary"),diameterFact:$("diameterFact"),compareLabel:$("compareLabel"),compareFact:$("compareFact"),distanceFact:$("distanceFact"),magFact:$("magFact"),positionFact:$("positionFact"),spectralFact:$("spectralFact"),ratioBar:$("ratioBar"),tabPanel:$("tabPanel"),finder:$("finder"),search:$("search"),results:$("results"),calibrate:$("calibrate"),calStar:$("calStar"),calReadout:$("calReadout"),atlas:$("atlas"),atlasSearch:$("atlasSearch"),atlasResults:$("atlasResults"),orrery:$("orrery"),voyager:$("voyager3d"),spaceScene:$("spaceScene"),voyagerTitle:$("voyagerTitle"),voyagerText:$("voyagerText"),voyagerProgress:$("voyagerProgress")};
function norm(a){return((a%360)+360)%360}
function delta(t,c){let d=norm(t)-norm(c);if(d>180)d-=360;if(d<-180)d+=360;return d}
function clamp(v,a,b){return Math.max(a,Math.min(b,v))}
function toast(m){el.toast.textContent=m;el.toast.classList.add("show");clearTimeout(toast.t);toast.t=setTimeout(()=>el.toast.classList.remove("show"),2300)}
function guide(m){el.guide.textContent=m}
function objectById(id){return objects.find(o=>o.id===id)||DATA.bodies.find(o=>o.id===id)||DATA.stars.find(o=>o.id===id)||DATA.catalog.find(o=>o.id===id)}
function ratioEarth(o){return o.diameterKm?o.diameterKm/EARTH:null}
function ratioSun(o){return o.diameterKm?o.diameterKm/SUN:null}
function arSize(o){const r=ratioEarth(o)||1; if(o.group==="Milky Way") return clamp(18 + Math.log10(Math.max(r,1))*12,22,70); return clamp(28 + Math.pow(r,.45)*16,22,92)}
function compareRule(o){const rE=ratioEarth(o),rS=ratioSun(o); if(o.id==="sun") return {label:"vs Earth",value:rE?`${rE.toFixed(1)}×`:"N/A",ref:"earth",mode:"Sun compared with Earth only"}; if(rS && rS>1) return {label:"vs Sun",value:`${rS.toFixed(rS>10?1:2)}×`,ref:"sun",mode:"Larger than Sun: compared with Sun"}; return {label:"vs Earth",value:rE?`${rE.toFixed(rE>10?1:2)}×`:"N/A",ref:"earth",mode:"Compared with Earth"}}
function scaleSizes(o){const rule=compareRule(o); if(rule.ref==="sun"){const r=ratioSun(o)||1; return {selected:clamp(95*Math.pow(r,.45),95,190),ref:75,bar:clamp(Math.log10(r+1)*50,8,100)}} const r=ratioEarth(o)||1; if(o.id==="sun") return {selected:170,ref:clamp(170*(EARTH/SUN),3,12),bar:100}; if(r<1) return {selected:clamp(120*r,8,120),ref:120,bar:clamp(r*100,6,100)}; return {selected:clamp(45+Math.pow(r,.5)*30,35,175),ref:32,bar:clamp(Math.log10(r+1)*35,8,100)}}
function currentHeading(){return norm(state.heading+state.headingOffset)}
function currentPitch(){return state.pitch+state.pitchOffset}
function posFor(o){const dx=delta(o.az,currentHeading()),dy=o.alt-currentPitch();return{x:50+(dx/75)*50,y:50-(dy/45)*50,dx,dy,visible:Math.abs(dx)<75&&Math.abs(dy)<45}}
function buildSky(){el.sky.innerHTML="";objects.forEach(o=>{const b=document.createElement("button");b.className="sky-object";b.type="button";b.dataset.id=o.id;const isStar=o.group==="Milky Way";b.innerHTML=isStar?`<span class="star-dot texture-${o.texture}"></span><span class="object-label"><strong>${o.name}</strong><small>${o.constellation||o.type}</small></span>`:`<span class="orb texture-${o.texture}"></span><span class="object-label"><strong>${o.name}</strong><small>${o.type}</small></span>`;b.onclick=()=>openObject(o.id,true);el.sky.appendChild(b)})}
function render(){state.heading=norm(state.heading+delta(state.rawHeading,state.heading)*.16);state.pitch+=(state.rawPitch-state.pitch)*.16;renderObjects();renderEdges();renderHud();drawStars();requestAnimationFrame(render)}
function renderObjects(){objects.forEach(o=>{const n=el.sky.querySelector(`[data-id="${o.id}"]`);if(!n)return;const p=posFor(o),edge=Math.max(Math.abs(p.dx)/75,Math.abs(p.dy)/45);n.style.setProperty("--x",p.x+"%");n.style.setProperty("--y",p.y+"%");n.style.setProperty("--s",clamp(1.05-edge*.12,.72,1.12));n.style.setProperty("--size",arSize(o)+"px");n.classList.toggle("visible",p.visible || (!state.motion && state.started));n.classList.toggle("selected",state.selected===o.id)})}
function renderEdges(){el.edge.innerHTML="";objects.map(o=>({...posFor(o),o,score:Math.abs(posFor(o).dx)+Math.abs(posFor(o).dy)*.8})).filter(x=>!x.visible).sort((a,b)=>a.o.id===state.locked?-1:b.o.id===state.locked?1:a.score-b.score).slice(0,7).forEach(item=>{const e=document.createElement("button");e.className="edge";e.type="button";const mx=innerWidth<740?52:76,my=innerWidth<740?104:90;let x=50+(item.dx/150)*50,y=50-(item.dy/90)*50;x=clamp(x,mx/innerWidth*100,100-mx/innerWidth*100);y=clamp(y,my/innerHeight*100,100-my/innerHeight*100);const arrow=Math.abs(item.dx)>Math.abs(item.dy)?item.dx>0?"→":"←":item.dy>0?"↑":"↓";e.style.left=x+"%";e.style.top=y+"%";e.innerHTML=`<i></i><span><strong>${item.o.name}</strong><small>${item.dx>0?"right":"left"} ${Math.round(Math.abs(item.dx))}°</small></span><b>${arrow}</b>`;e.onclick=()=>openObject(item.o.id,true);el.edge.appendChild(e)})}
function renderHud(){el.heading.textContent=`HDG ${Math.round(currentHeading())}°`;if(state.locked){const o=objectById(state.locked),p=posFor(o);guide(Math.abs(p.dx)<6&&Math.abs(p.dy)<6?`${o.name} centered.`:`Target ${o.name}: ${p.dx>0?"turn right":"turn left"} ${Math.round(Math.abs(p.dx))}°, ${p.dy>0?"tilt up":"tilt down"} ${Math.round(Math.abs(p.dy))}°.`)}}
function openObject(id,show){const o=objectById(id);if(!o)return;if(!objects.some(x=>x.id===id)&&id.startsWith("cat-")){objects.push(o);buildSky()}state.selected=id;el.objectGroup.textContent=o.group||o.type;el.objectName.textContent=o.name;el.objectSummary.textContent=o.summary;el.diameterFact.textContent=o.diameterKm?o.diameterKm.toLocaleString()+" km":"Unknown";const rule=compareRule(o);el.compareLabel.textContent=rule.label;el.compareFact.textContent=rule.value;el.distanceFact.textContent=o.distance||"Unknown";el.magFact.textContent=o.mag!=null?String(o.mag):"—";el.spectralFact.textContent=o.spectral||"—";const p=posFor(o);el.positionFact.textContent=`${p.dx>0?"Right":"Left"} ${Math.round(Math.abs(p.dx))}° / ${p.dy>0?"Up":"Down"} ${Math.round(Math.abs(p.dy))}°`;el.selectedSphere.className=`sphere texture-${o.texture||"moon"}`;el.referenceSphere.className=`sphere texture-${rule.ref}`;el.referenceLabel.textContent=rule.ref==="sun"?"Sun":"Earth";el.selectedLabel.textContent=o.name;const s=scaleSizes(o);el.selectedSphere.style.setProperty("--size",s.selected+"px");el.referenceSphere.style.setProperty("--size",s.ref+"px");el.ratioBar.style.width=s.bar+"%";el.scaleMode.textContent=rule.mode;renderTab(o);if(show){el.inspector.classList.add("open");el.finder.classList.remove("open");el.atlas.classList.remove("open");el.voyager.classList.remove("open")}}
function renderTab(o){document.querySelectorAll(".tab").forEach(t=>t.classList.toggle("active",t.dataset.tab===state.tab));if(state.tab==="story")el.tabPanel.textContent=o.summary;if(state.tab==="expert")el.tabPanel.innerHTML=`<b>Type:</b> ${o.type}<br><b>Group:</b> ${o.group||"Catalogue"}<br><b>Constellation:</b> ${o.constellation||"—"}<br><b>Spectral class:</b> ${o.spectral||"—"}<br><b>Expert note:</b> ${o.science||"Prototype object."}<br><b>Detail:</b> ${o.detail||"—"}`;if(state.tab==="scale"){const rule=compareRule(o);el.tabPanel.textContent=`Scale rule: ${o.name} is ${rule.value} ${rule.label.replace("vs ","")} reference. The AR size is proportional but screen-safe, so Jupiter appears larger than the Moon without filling the screen.`}}
function launchAR(){state.started=true;state.demo=false;el.start.classList.add("hide");el.voyager.classList.remove("open");setRail("modeAR");toast("AR launched");guide("AR is primary. Enable Motion for phone movement tracking.");}
async function startCamera(){launchAR();try{const s=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:"environment"}},audio:false});el.camera.srcObject=s;el.camera.style.display="block";toast("Camera active")}catch(e){toast("Camera unavailable; AR overlay remains active")}}
async function motion(){try{if(typeof DeviceOrientationEvent!=="undefined"&&typeof DeviceOrientationEvent.requestPermission==="function"){const p=await DeviceOrientationEvent.requestPermission();if(p!=="granted"){toast("Motion denied");return}}window.addEventListener("deviceorientation",onOrientation,true);state.motion=true;el.motionChip.textContent="Motion on";toast("Motion tracking enabled")}catch(e){toast("Motion unavailable")}}
function onOrientation(e){let h=null;if(typeof e.webkitCompassHeading==="number")h=e.webkitCompassHeading;else if(typeof e.alpha==="number")h=360-e.alpha;if(h!==null)state.rawHeading=norm(h);if(typeof e.beta==="number"){state.rawPitch=clamp(90-Math.abs(e.beta),-20,80)}}
function gps(){if(!navigator.geolocation){toast("GPS unavailable");return}navigator.geolocation.getCurrentPosition(p=>{state.lat=p.coords.latitude;state.lon=p.coords.longitude;el.gps.textContent="GPS on";updateLiveSky(state.lat,state.lon);toast("GPS refined sky positions")},()=>toast("GPS denied; fallback remains active"),{enableHighAccuracy:true,timeout:10000})}
function updateLiveSky(lat,lon){const sun=objectById("sun"),moon=objectById("moon");Object.assign(sun,solar(new Date(),lat,lon));Object.assign(moon,moonApprox(new Date(),lat,lon));DATA.stars.forEach(s=>Object.assign(s,raDecToAltAz(new Date(),lat,lon,s.ra,s.dec)))}
function gmst(date){const d=(date-Date.UTC(2000,0,1,12))/86400000;return norm(280.46061837+360.98564736629*d)}
function raDecToAltAz(date,lat,lon,raHours,decDeg){const rad=Math.PI/180,deg=180/Math.PI;let ha=norm(gmst(date)+lon-raHours*15)*rad;if(ha>Math.PI)ha-=2*Math.PI;const dec=decDeg*rad,la=lat*rad;const alt=Math.asin(Math.sin(la)*Math.sin(dec)+Math.cos(la)*Math.cos(dec)*Math.cos(ha));const az=Math.atan2(-Math.sin(ha),Math.tan(dec)*Math.cos(la)-Math.sin(la)*Math.cos(ha));return{az:norm(az*deg),alt:clamp(alt*deg,-30,85)}}
function solar(date,lat,lon){const rad=Math.PI/180,day=Math.floor((date-Date.UTC(date.getUTCFullYear(),0,0))/86400000),h=date.getUTCHours()+date.getUTCMinutes()/60;const g=2*Math.PI/365*(day-1+(h-12)/24);const dec=.006918-.399912*Math.cos(g)+.070257*Math.sin(g)-.006758*Math.cos(2*g)+.000907*Math.sin(2*g)-.002697*Math.cos(3*g)+.00148*Math.sin(3*g);const eq=229.18*(.000075+.001868*Math.cos(g)-.032077*Math.sin(g)-.014615*Math.cos(2*g)-.040849*Math.sin(2*g));return horizontal(date,lat,lon,dec,eq)}
function moonApprox(date,lat,lon){const d=(date-Date.UTC(2000,0,1,12))/86400000,rad=Math.PI/180;const L=norm(218.316+13.176396*d),M=norm(134.963+13.064993*d),F=norm(93.272+13.229350*d);const lm=(L+6.289*Math.sin(M*rad))*rad,bm=5.128*Math.sin(F*rad)*rad,eps=23.439*rad;const dec=Math.asin(Math.sin(bm)*Math.cos(eps)+Math.cos(bm)*Math.sin(eps)*Math.sin(lm));const ra=Math.atan2(Math.sin(lm)*Math.cos(eps)-Math.tan(bm)*Math.sin(eps),Math.cos(lm));return horizontal(date,lat,lon,dec,0,ra)}
function horizontal(date,lat,lon,dec,eq=0,ra=null){const rad=Math.PI/180,deg=180/Math.PI;let ha;if(ra==null){const tst=(date.getUTCHours()*60+date.getUTCMinutes()+date.getUTCSeconds()/60+eq+4*lon)%1440;ha=(tst/4<0?tst/4+180:tst/4-180)*rad}else{ha=norm(gmst(date)+lon-ra*deg)*rad;if(ha>Math.PI)ha-=2*Math.PI}const la=lat*rad,alt=Math.asin(Math.sin(la)*Math.sin(dec)+Math.cos(la)*Math.cos(dec)*Math.cos(ha));const az=Math.atan2(-Math.sin(ha),Math.tan(dec)*Math.cos(la)-Math.sin(la)*Math.cos(ha));return{az:norm(az*deg),alt:clamp(alt*deg,-30,85)}}
function openCalibrate(){el.calibrate.classList.add("open");setRail("modeCal");el.calStar.innerHTML="";DATA.stars.slice().sort((a,b)=>a.mag-b.mag).forEach(s=>{const opt=document.createElement("option");opt.value=s.id;opt.textContent=`${s.name} · ${s.constellation} · mag ${s.mag}`;el.calStar.appendChild(opt)});el.calReadout.textContent=`Current calibration: heading offset ${Math.round(state.headingOffset)}°, pitch offset ${Math.round(state.pitchOffset)}°.`}
function alignStar(){const s=objectById(el.calStar.value);if(!s)return;state.headingOffset=delta(state.rawHeading,s.az);state.pitchOffset=s.alt-state.rawPitch;el.calChip.textContent=`CAL ${s.name}`;el.calReadout.textContent=`Aligned to ${s.name}. Heading offset ${Math.round(state.headingOffset)}°, pitch offset ${Math.round(state.pitchOffset)}°.`;toast(`Aligned to ${s.name}`)}
function resetCal(){state.headingOffset=0;state.pitchOffset=0;el.calChip.textContent="CAL off";el.calReadout.textContent="Calibration reset.";toast("Calibration reset")}
function renderFinder(){const q=el.search.value.toLowerCase().trim();let list=[];if(state.filter==="visible")list=objects;if(state.filter==="stars")list=DATA.stars.slice().sort((a,b)=>a.mag-b.mag);if(state.filter==="solar")list=DATA.bodies;if(state.filter==="all")list=[...objects,...DATA.catalog.slice(0,420)];if(q)list=[...objects,...DATA.catalog].filter(o=>(o.name+" "+o.type+" "+(o.constellation||"")+" "+(o.spectral||"")+" "+(o.science||"")).toLowerCase().includes(q)).slice(0,180);el.results.innerHTML="";list.forEach(o=>el.results.appendChild(card(o,()=>openObject(o.id,true))))}
function card(o,fn){const c=document.createElement("button");c.type="button";c.className="result-card";c.innerHTML=`<span class="result-dot"></span><span><strong>${o.name}</strong><small>${o.type} · ${o.constellation||o.group||"Atlas"}${o.mag!=null?" · mag "+o.mag:""}</small></span><b>→</b>`;c.onclick=fn;return c}
function openAtlas(){el.atlas.classList.add("open");renderAtlas();setRail("modeAtlas")}
function renderAtlas(){renderOrrery();const q=el.atlasSearch.value.toLowerCase().trim();const list=[...DATA.bodies,...DATA.stars,...DATA.catalog].filter(o=>(o.name+" "+o.type+" "+(o.constellation||"")+" "+(o.spectral||"")+" "+(o.science||"")).toLowerCase().includes(q)).slice(0,240);el.atlasResults.innerHTML="";list.forEach(o=>el.atlasResults.appendChild(card(o,()=>openObject(o.id,true))))}
function renderOrrery(){el.orrery.innerHTML="";DATA.bodies.filter(o=>["mercury","venus","earth","mars","jupiter","saturn","uranus","neptune"].includes(o.id)).forEach((o,i)=>{const size=72+i*37,path=document.createElement("div");path.className="orbit-path";path.style.width=size+"%";path.style.height=size+"%";el.orrery.appendChild(path);const b=document.createElement("button");b.className=`orbit-body texture-${o.texture}`;const angle=(i*48+Date.now()/80000*25)%360,r=size/2;b.style.left=`${50+Math.cos(angle*Math.PI/180)*r/2}%`;b.style.top=`${50+Math.sin(angle*Math.PI/180)*r/2}%`;const s=clamp(arSize(o)/2,7,34);b.style.width=s+"px";b.style.height=s+"px";b.onclick=()=>openObject(o.id,true);el.orrery.appendChild(b)});const sun=document.createElement("button");sun.className="orbit-body texture-sun";sun.style.left="50%";sun.style.top="50%";sun.style.width="36px";sun.style.height="36px";sun.onclick=()=>openObject("sun",true);el.orrery.appendChild(sun)}
function openVoyager(routeId="voyager"){el.voyager.classList.add("open");setRail("modeVoyager");state.voyagerRoute=routeId;state.voyagerStep=0;state.voyagerRunning=true;renderVoyager();clearInterval(state.voyagerTimer);state.voyagerTimer=setInterval(()=>{if(state.voyagerRunning)nextVoyager()},5000)}
function renderVoyager(){const route=DATA.journeys.find(j=>j.id===state.voyagerRoute)||DATA.journeys[0];const target=objectById(route.steps[state.voyagerStep]);el.voyagerTitle.textContent=route.title;el.voyagerText.textContent=`Flyby ${state.voyagerStep+1}/${route.steps.length}: ${target.name}. ${target.summary}`;el.voyagerProgress.style.width=((state.voyagerStep+1)/route.steps.length*100)+"%";el.spaceScene.innerHTML="";route.steps.forEach((id,i)=>{const o=objectById(id),d=document.createElement("div"),lab=document.createElement("div");d.className=`fly-planet texture-${o.texture}`;const offset=i-state.voyagerStep;const z=-offset*520;const x=offset*80;const y=Math.sin(i*1.7)*70;const size=clamp(arSize(o)*1.25,24,170);d.style.width=size+"px";d.style.height=size+"px";d.style.transform=`translate3d(calc(-50% + ${x}px), calc(-50% + ${y}px), ${z}px)`;d.style.opacity=Math.abs(offset)>4?.08:1;lab.className="fly-label";lab.textContent=o.name;lab.style.transform=`translate3d(calc(-50% + ${x}px), calc(-50% + ${y+size/2+26}px), ${z}px)`;lab.style.opacity=d.style.opacity;el.spaceScene.appendChild(d);el.spaceScene.appendChild(lab)});openObject(target.id,false)}
function nextVoyager(){const route=DATA.journeys.find(j=>j.id===state.voyagerRoute)||DATA.journeys[0];state.voyagerStep=(state.voyagerStep+1)%route.steps.length;renderVoyager()}
function prevVoyager(){const route=DATA.journeys.find(j=>j.id===state.voyagerRoute)||DATA.journeys[0];state.voyagerStep=(state.voyagerStep-1+route.steps.length)%route.steps.length;renderVoyager()}
function pauseVoyager(){state.voyagerRunning=!state.voyagerRunning;$("pauseVoyager").textContent=state.voyagerRunning?"Pause":"Resume";toast(state.voyagerRunning?"Voyager resumed":"Voyager paused")}
function setRail(id){document.querySelectorAll(".rail-btn").forEach(r=>r.classList.remove("active"));$(id).classList.add("active")}
function setupStars(){const c=el.starfield,r=Math.min(devicePixelRatio||1,2);c.width=Math.floor(innerWidth*r);c.height=Math.floor(innerHeight*r);c.style.width=innerWidth+"px";c.style.height=innerHeight+"px";state.stars=Array.from({length:450},()=>({x:Math.random(),y:Math.random(),r:Math.random()*1.4+.2,a:Math.random()*.75+.15}))}
function drawStars(){const c=el.starfield,ctx=c.getContext("2d");if(!ctx)return;const r=Math.min(devicePixelRatio||1,2);ctx.clearRect(0,0,c.width,c.height);ctx.save();ctx.scale(r,r);const px=delta(currentHeading(),180)*.28,py=currentPitch()*-.55;state.stars.forEach(s=>{const x=((s.x*innerWidth+px)%innerWidth+innerWidth)%innerWidth,y=((s.y*innerHeight+py)%innerHeight+innerHeight)%innerHeight;ctx.globalAlpha=s.a;ctx.fillStyle="#fff";ctx.beginPath();ctx.arc(x,y,s.r,0,Math.PI*2);ctx.fill()});ctx.restore()}
document.addEventListener("click",e=>{const close=e.target.closest("[data-close]");if(close)$(close.dataset.close).classList.remove("open")});
$("launchBtn").onclick=()=>{launchAR();motion()};$("cameraBtn").onclick=startCamera;$("motionBtn").onclick=motion;$("gpsBtn").onclick=gps;$("calBtn").onclick=openCalibrate;$("voyagerBtn").onclick=()=>openVoyager("voyager");$("voyagerObjectBtn").onclick=()=>openVoyager("voyager");$("finderBtn").onclick=()=>{el.finder.classList.add("open");renderFinder()};$("modeAtlas").onclick=openAtlas;$("modeVoyager").onclick=()=>openVoyager("voyager");$("modeCal").onclick=openCalibrate;$("modeAR").onclick=()=>{el.atlas.classList.remove("open");el.voyager.classList.remove("open");el.calibrate.classList.remove("open");setRail("modeAR")};$("lockBtn").onclick=()=>{state.locked=state.selected;toast("Target locked")};$("nextVoyager").onclick=nextVoyager;$("prevVoyager").onclick=prevVoyager;$("pauseVoyager").onclick=pauseVoyager;$("backToAR").onclick=()=>el.atlas.classList.remove("open");$("alignStarBtn").onclick=alignStar;$("resetCalBtn").onclick=resetCal;$("brand").onclick=()=>guide(`${VERSION}. AR primary, 3D Voyager optional.`);el.search.oninput=renderFinder;el.atlasSearch.oninput=renderAtlas;document.querySelectorAll(".filter").forEach(f=>f.onclick=()=>{document.querySelectorAll(".filter").forEach(x=>x.classList.remove("active"));f.classList.add("active");state.filter=f.dataset.filter;renderFinder()});document.querySelectorAll(".tab").forEach(t=>t.onclick=()=>{state.tab=t.dataset.tab;renderTab(objectById(state.selected))});window.addEventListener("resize",setupStars);
buildSky();setupStars();updateLiveSky(DEFAULT_LAT,DEFAULT_LON);renderFinder();renderAtlas();openObject("sun",false);requestAnimationFrame(render);
