
const DATA=window.SKY_EXPLORER_DATA,VERSION=DATA.version,EARTH=12742,SUN=1392700;
let objects=[...DATA.bodies.filter(o=>o.id!=="earth"),...DATA.stars];
const state={heading:95,rawHeading:95,pitch:18,rawPitch:18,demo:false,motion:false,selected:"sun",locked:null,tab:"story",filter:"bright",stars:[],journey:null,step:0,flightFrom:null,flightTo:null};
const $=id=>document.getElementById(id);
const el={start:$("start"),camera:$("camera"),starfield:$("starfield"),sky:$("skyLayer"),edge:$("edgeLayer"),guide:$("guideText"),toast:$("toast"),versionText:$("versionText"),versionChip:$("versionChip"),heading:$("headingChip"),gps:$("gpsChip"),inspector:$("inspector"),selectedSphere:$("selectedSphere"),earthSphere:$("earthSphere"),sunSphere:$("sunSphere"),selectedLabel:$("selectedLabel"),scaleMode:$("scaleMode"),objectGroup:$("objectGroup"),objectName:$("objectName"),objectSummary:$("objectSummary"),diameterFact:$("diameterFact"),earthRatioFact:$("earthRatioFact"),sunRatioFact:$("sunRatioFact"),distanceFact:$("distanceFact"),magFact:$("magFact"),positionFact:$("positionFact"),ratioBar:$("ratioBar"),tabPanel:$("tabPanel"),finder:$("finder"),search:$("search"),results:$("results"),journey:$("journey"),journeyTitle:$("journeyTitle"),journeyDesc:$("journeyDesc"),journeyProgress:$("journeyProgress"),journeyStep:$("journeyStep"),journeyChoices:$("journeyChoices"),atlas:$("atlas"),atlasSearch:$("atlasSearch"),atlasResults:$("atlasResults"),orrery:$("orrery"),flightSvg:$("flightSvg"),flightPath:$("flightPath"),ship:$("ship")};
el.versionText.textContent=VERSION;el.versionChip.textContent="2B Flightpath";
function norm(a){return((a%360)+360)%360}function delta(t,c){let d=norm(t)-norm(c);if(d>180)d-=360;if(d<-180)d+=360;return d}function clamp(v,a,b){return Math.max(a,Math.min(b,v))}
function ratioEarth(o){return o.diameterKm?o.diameterKm/EARTH:null}function ratioSun(o){return o.diameterKm?o.diameterKm/SUN:null}
function scaleSizes(o){const rE=ratioEarth(o)||1,rS=ratioSun(o)||0.01;let selected=90,earth=32,sun=130;if(rE<1){earth=90;selected=clamp(90*rE,8,90);sun=150}else if(rS>1){sun=70;selected=clamp(70*Math.pow(rS,.55),80,190);earth=clamp(70*(EARTH/SUN),3,12)}else{selected=clamp(150*(o.diameterKm/SUN),8,150);sun=150;earth=clamp(150*(EARTH/SUN),3,16)}return{selected,earth,sun,bar:clamp((Math.log10(rE)+1.5)/4*100,4,100),mode:rS>1?"Object vs Sun + Earth":rE<1?"Smaller than Earth":"Object vs Sun"}}
function toast(m){el.toast.textContent=m;el.toast.classList.add("show");clearTimeout(toast.t);toast.t=setTimeout(()=>el.toast.classList.remove("show"),2100)}function guide(m){el.guide.textContent=m}
function objectById(id){return objects.find(o=>o.id===id)||DATA.bodies.find(o=>o.id===id)||DATA.stars.find(o=>o.id===id)||DATA.catalog.find(o=>o.id===id)}
function visibleObjects(){return objects}
function buildSky(){el.sky.innerHTML="";visibleObjects().forEach(o=>{const b=document.createElement("button");b.className="sky-object";b.type="button";b.dataset.id=o.id;const isStar=o.group==="Milky Way";b.innerHTML=isStar?`<span class="star-dot texture-${o.texture}"></span><span class="object-label"><strong>${o.name}</strong><small>${o.type}</small></span>`:`<span class="orb texture-${o.texture}"></span><span class="object-label"><strong>${o.name}</strong><small>${o.type}</small></span>`;b.onclick=()=>openObject(o.id,true);el.sky.appendChild(b)})}
function posFor(o){const dx=delta(o.az,state.heading),dy=o.alt-state.pitch;return{x:50+(dx/31)*50,y:50-(dy/25)*50,dx,dy,visible:Math.abs(dx)<31&&Math.abs(dy)<25}}
function render(){if(state.demo&&!state.motion){const t=performance.now()/1000;state.rawHeading=norm(110+Math.sin(t*.25)*170+t*2.4);state.rawPitch=24+Math.sin(t*.52)*18}state.heading=norm(state.heading+delta(state.rawHeading,state.heading)*.12);state.pitch+=(state.rawPitch-state.pitch)*.14;renderObjects();renderEdges();renderFlight();renderHud();drawStars();requestAnimationFrame(render)}
function renderObjects(){visibleObjects().forEach(o=>{const n=el.sky.querySelector(`[data-id="${o.id}"]`);if(!n)return;const p=posFor(o),edge=Math.max(Math.abs(p.dx)/31,Math.abs(p.dy)/25);n.style.setProperty("--x",p.x+"%");n.style.setProperty("--y",p.y+"%");n.style.setProperty("--s",clamp(1.08-edge*.18,.65,1.12));n.classList.toggle("visible",p.visible);n.classList.toggle("selected",state.selected===o.id)})}
function renderEdges(){el.edge.innerHTML="";visibleObjects().map(o=>({...posFor(o),o,score:Math.abs(posFor(o).dx)+Math.abs(posFor(o).dy)*.8})).filter(x=>!x.visible).sort((a,b)=>a.o.id===state.locked?-1:b.o.id===state.locked?1:a.score-b.score).slice(0,7).forEach(item=>{const e=document.createElement("button");e.className="edge";e.type="button";const mx=innerWidth<740?52:76,my=innerWidth<740?104:90;let x=50+(item.dx/140)*50,y=50-(item.dy/100)*50;x=clamp(x,mx/innerWidth*100,100-mx/innerWidth*100);y=clamp(y,my/innerHeight*100,100-my/innerHeight*100);if(Math.abs(item.dx)>Math.abs(item.dy)*1.1)x=item.dx<0?mx/innerWidth*100:100-mx/innerWidth*100;else y=item.dy>0?my/innerHeight*100:100-my/innerHeight*100;const arrow=Math.abs(item.dx)>Math.abs(item.dy)?item.dx>0?"→":"←":item.dy>0?"↑":"↓";e.style.left=x+"%";e.style.top=y+"%";e.innerHTML=`<i></i><span><strong>${item.o.name}</strong><small>${item.dx>0?"right":"left"} ${Math.round(Math.abs(item.dx))}°</small></span><b>${arrow}</b>`;e.onclick=()=>openObject(item.o.id,true);el.edge.appendChild(e)})}
function renderFlight(){if(!state.flightTo){el.flightSvg.classList.remove("active");el.ship.classList.remove("active");return}const to=objectById(state.flightTo),from=objectById(state.flightFrom)||{az:state.heading,alt:state.pitch};if(!to)return;const a=posFor(from),b=posFor(to);const ax=clamp(a.x,8,92),ay=clamp(a.y,12,88),bx=clamp(b.x,8,92),by=clamp(b.y,12,88);const mx=(ax+bx)/2,my=Math.min(88,Math.max(12,(ay+by)/2-18));el.flightPath.setAttribute("d",`M ${ax} ${ay} Q ${mx} ${my} ${bx} ${by}`);el.flightSvg.classList.add("active");const t=(performance.now()/1600)%1;const x=(1-t)*(1-t)*ax+2*(1-t)*t*mx+t*t*bx,y=(1-t)*(1-t)*ay+2*(1-t)*t*my+t*t*by;el.ship.style.left=x+"%";el.ship.style.top=y+"%";el.ship.classList.add("active")}
function renderHud(){el.heading.textContent=`HDG ${Math.round(state.heading)}°`;if(state.locked){const o=objectById(state.locked),p=posFor(o);guide(Math.abs(p.dx)<5&&Math.abs(p.dy)<5?`${o.name} centered. Flight target reached.`:`Flight target ${o.name}: ${p.dx>0?"turn right":"turn left"} ${Math.round(Math.abs(p.dx))}°, ${p.dy>0?"tilt up":"tilt down"} ${Math.round(Math.abs(p.dy))}°.`)}}
function openObject(id,show){const o=objectById(id);if(!o)return;if(!objects.some(x=>x.id===id)&&id.startsWith("cat-")){objects.push(o);buildSky()}state.selected=id;el.objectGroup.textContent=o.group||o.type;el.objectName.textContent=o.name;el.objectSummary.textContent=o.summary;el.diameterFact.textContent=o.diameterKm?o.diameterKm.toLocaleString()+" km":"Unknown";const rE=ratioEarth(o),rS=ratioSun(o);el.earthRatioFact.textContent=rE?rE.toFixed(rE>10?1:2)+"×":"N/A";el.sunRatioFact.textContent=rS?rS.toFixed(rS>10?1:3)+"×":"N/A";el.distanceFact.textContent=o.distance||"Unknown";el.magFact.textContent=o.mag!=null?String(o.mag):"—";const p=posFor(o);el.positionFact.textContent=`${p.dx>0?"Right":"Left"} ${Math.round(Math.abs(p.dx))}° / ${p.dy>0?"Up":"Down"} ${Math.round(Math.abs(p.dy))}°`;el.selectedSphere.className=`sphere texture-${o.texture||"moon"}`;el.selectedLabel.textContent=o.name;const s=scaleSizes(o);el.selectedSphere.style.setProperty("--size",s.selected+"px");el.earthSphere.style.setProperty("--size",s.earth+"px");el.sunSphere.style.setProperty("--size",s.sun+"px");el.ratioBar.style.width=s.bar+"%";el.scaleMode.textContent=s.mode;renderTab(o);guide(`${o.name}: ${o.story||o.summary}`);if(show){el.inspector.classList.add("open");el.finder.classList.remove("open");el.atlas.classList.remove("open")}}
function renderTab(o){document.querySelectorAll(".tab").forEach(t=>t.classList.toggle("active",t.dataset.tab===state.tab));if(state.tab==="story")el.tabPanel.textContent=o.story||o.summary;if(state.tab==="science")el.tabPanel.innerHTML=`<b>Type:</b> ${o.type}<br><b>Group:</b> ${o.group||"Catalogue"}<br><b>Constellation:</b> ${o.constellation||"—"}<br><b>Spectral class:</b> ${o.spectral||"—"}<br><b>Expert note:</b> ${o.science||"Prototype object."}`;if(state.tab==="scale")el.tabPanel.textContent=o.diameterKm?`${o.name} is ${(ratioEarth(o)).toFixed(ratioEarth(o)>10?1:2)} Earth diameters and ${(ratioSun(o)).toFixed(ratioSun(o)>10?1:3)} Sun diameters. Large stars are compared to both Earth and Sun to preserve scale.`:"No trusted diameter yet; shown as catalogue object."}
async function startCamera(){el.start.classList.add("hide");try{const s=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:"environment"}},audio:false});el.camera.srcObject=s;el.camera.style.display="block";toast("Camera active")}catch(e){toast("Camera unavailable; visual AR active")}}
async function motion(){try{if(typeof DeviceOrientationEvent!=="undefined"&&typeof DeviceOrientationEvent.requestPermission==="function"){const p=await DeviceOrientationEvent.requestPermission();if(p!=="granted"){toast("Motion denied");return}}window.addEventListener("deviceorientation",e=>{if(typeof e.alpha==="number")state.rawHeading=norm(e.alpha);if(typeof e.beta==="number")state.rawPitch=clamp(90-Math.abs(e.beta),-25,75)},true);state.motion=true;state.demo=false;toast("Motion tracking enabled")}catch(e){toast("Motion unavailable")}}
function demo(){el.start.classList.add("hide");state.demo=true;toast("Demo AR launched");guide("Demo AR active. Start Flight Journey to see the red route and spaceship.")}
function gps(){if(!navigator.geolocation){toast("GPS unavailable");return}navigator.geolocation.getCurrentPosition(p=>{el.gps.textContent="GPS on";updateLiveSky(p.coords.latitude,p.coords.longitude);toast("GPS refined Sun, Moon and bright stars")},()=>toast("GPS denied"))}
function updateLiveSky(lat,lon){const sun=objectById("sun"),moon=objectById("moon");Object.assign(sun,solar(new Date(),lat,lon));Object.assign(moon,moonApprox(new Date(),lat,lon));DATA.stars.forEach(s=>Object.assign(s,raDecToAltAz(new Date(),lat,lon,s.ra,s.dec)));}
function gmst(date){const d=(date-Date.UTC(2000,0,1,12))/86400000;return norm(280.46061837+360.98564736629*d)}
function raDecToAltAz(date,lat,lon,raHours,decDeg){const rad=Math.PI/180,deg=180/Math.PI;let ha=norm(gmst(date)+lon-raHours*15)*rad;if(ha>Math.PI)ha-=2*Math.PI;const dec=decDeg*rad,la=lat*rad;const alt=Math.asin(Math.sin(la)*Math.sin(dec)+Math.cos(la)*Math.cos(dec)*Math.cos(ha));const az=Math.atan2(-Math.sin(ha),Math.tan(dec)*Math.cos(la)-Math.sin(la)*Math.cos(ha));return{az:norm(az*deg),alt:clamp(alt*deg,-30,85)}}
function solar(date,lat,lon){const rad=Math.PI/180,day=Math.floor((date-Date.UTC(date.getUTCFullYear(),0,0))/86400000),h=date.getUTCHours()+date.getUTCMinutes()/60;const g=2*Math.PI/365*(day-1+(h-12)/24);const dec=.006918-.399912*Math.cos(g)+.070257*Math.sin(g)-.006758*Math.cos(2*g)+.000907*Math.sin(2*g)-.002697*Math.cos(3*g)+.00148*Math.sin(3*g);const eq=229.18*(.000075+.001868*Math.cos(g)-.032077*Math.sin(g)-.014615*Math.cos(2*g)-.040849*Math.sin(2*g));return horizontal(date,lat,lon,dec,eq)}
function moonApprox(date,lat,lon){const d=(date-Date.UTC(2000,0,1,12))/86400000,rad=Math.PI/180;const L=norm(218.316+13.176396*d),M=norm(134.963+13.064993*d),F=norm(93.272+13.229350*d);const lm=(L+6.289*Math.sin(M*rad))*rad,bm=5.128*Math.sin(F*rad)*rad,eps=23.439*rad;const dec=Math.asin(Math.sin(bm)*Math.cos(eps)+Math.cos(bm)*Math.sin(eps)*Math.sin(lm));const ra=Math.atan2(Math.sin(lm)*Math.cos(eps)-Math.tan(bm)*Math.sin(eps),Math.cos(lm));return horizontal(date,lat,lon,dec,0,ra)}
function horizontal(date,lat,lon,dec,eq=0,ra=null){const rad=Math.PI/180,deg=180/Math.PI;let ha;if(ra==null){const tst=(date.getUTCHours()*60+date.getUTCMinutes()+date.getUTCSeconds()/60+eq+4*lon)%1440;ha=(tst/4<0?tst/4+180:tst/4-180)*rad}else{ha=norm(gmst(date)+lon-ra*deg)*rad;if(ha>Math.PI)ha-=2*Math.PI}const la=lat*rad,alt=Math.asin(Math.sin(la)*Math.sin(dec)+Math.cos(la)*Math.cos(dec)*Math.cos(ha));const az=Math.atan2(-Math.sin(ha),Math.tan(dec)*Math.cos(la)-Math.sin(la)*Math.cos(ha));return{az:norm(az*deg),alt:clamp(alt*deg,-30,85)}}
function renderFinder(){const q=el.search.value.toLowerCase().trim();let list=[];if(state.filter==="bright")list=DATA.stars;if(state.filter==="solar")list=DATA.bodies;if(state.filter==="deep")list=DATA.catalog.filter(o=>["Galaxy","Nebula","Exoplanet System","Supernova Remnant"].includes(o.type)).slice(0,260);if(state.filter==="all")list=[...objects,...DATA.catalog.slice(0,420)];if(q)list=[...objects,...DATA.catalog].filter(o=>(o.name+" "+o.type+" "+(o.constellation||"")+" "+(o.spectral||"")).toLowerCase().includes(q)).slice(0,140);el.results.innerHTML="";list.forEach(o=>el.results.appendChild(card(o,()=>openObject(o.id,true))))}
function card(o,fn){const c=document.createElement("button");c.type="button";c.className="result-card";c.innerHTML=`<span class="result-dot"></span><span><strong>${o.name}</strong><small>${o.type} · ${o.constellation||o.group||"Atlas"}${o.mag!=null?" · mag "+o.mag:""}</small></span><b>→</b>`;c.onclick=fn;return c}
function openAtlas(){el.start.classList.add("hide");el.atlas.classList.add("open");renderAtlas();setRail("modeAtlas")}
function renderAtlas(){renderOrrery();const q=el.atlasSearch.value.toLowerCase().trim();const list=[...DATA.bodies,...DATA.stars,...DATA.catalog].filter(o=>(o.name+" "+o.type+" "+(o.constellation||"")+" "+(o.spectral||"")).toLowerCase().includes(q)).slice(0,180);el.atlasResults.innerHTML="";list.forEach(o=>el.atlasResults.appendChild(card(o,()=>openObject(o.id,true))))}
function renderOrrery(){el.orrery.innerHTML="";DATA.bodies.filter(o=>["mercury","venus","earth","mars","jupiter","saturn","uranus","neptune"].includes(o.id)).forEach((o,i)=>{const size=72+i*37,path=document.createElement("div");path.className="orbit-path";path.style.width=size+"%";path.style.height=size+"%";el.orrery.appendChild(path);const b=document.createElement("button");b.className=`orbit-body texture-${o.texture}`;const angle=(i*48+Date.now()/80000*25)%360,r=size/2;b.style.left=`${50+Math.cos(angle*Math.PI/180)*r/2}%`;b.style.top=`${50+Math.sin(angle*Math.PI/180)*r/2}%`;const s=clamp(scaleSizes(o).selected/4,7,34);b.style.width=s+"px";b.style.height=s+"px";b.onclick=()=>openObject(o.id,true);el.orrery.appendChild(b)});const sun=document.createElement("button");sun.className="orbit-body texture-sun";sun.style.left="50%";sun.style.top="50%";sun.style.width="36px";sun.style.height="36px";sun.onclick=()=>openObject("sun",true);el.orrery.appendChild(sun)}
function startJourney(id="scale"){const j=DATA.journeys.find(x=>x.id===id)||DATA.journeys[0];state.journey=j;state.step=0;el.start.classList.add("hide");el.journey.classList.add("open");setRail("modeJourney");renderJourney()}
function renderJourney(){const j=state.journey;if(!j)return;const target=objectById(j.steps[state.step])||DATA.catalog.find(x=>x.id===j.steps[state.step]);const prev=objectById(j.steps[Math.max(0,state.step-1)])||{az:state.heading,alt:state.pitch};el.journeyTitle.textContent=j.title;el.journeyDesc.textContent=j.description;el.journeyProgress.style.width=((state.step+1)/j.steps.length*100)+"%";el.journeyStep.innerHTML=`<b>Flight ${state.step+1}/${j.steps.length}: ${target.name}</b><br>${target.summary||target.type}<br><span class="kicker">Follow the red line and spaceship in AR.</span>`;el.journeyChoices.innerHTML="";DATA.journeys.forEach(x=>el.journeyChoices.appendChild(card({name:x.title,type:x.description,group:"Journey"},()=>startJourney(x.id))));state.flightFrom=prev.id;state.flightTo=target.id;state.locked=target.id;openObject(target.id,false);guide(`AR Flight: follow the red route to ${target.name}.`) }
function nextStep(){if(!state.journey)return startJourney();state.step=(state.step+1)%state.journey.steps.length;renderJourney()}function prevStep(){if(!state.journey)return;state.step=(state.step-1+state.journey.steps.length)%state.journey.steps.length;renderJourney()}
function setRail(id){document.querySelectorAll(".rail-btn").forEach(r=>r.classList.remove("active"));$(id).classList.add("active")}
function setupStars(){const c=el.starfield,r=Math.min(devicePixelRatio||1,2);c.width=Math.floor(innerWidth*r);c.height=Math.floor(innerHeight*r);c.style.width=innerWidth+"px";c.style.height=innerHeight+"px";state.stars=Array.from({length:420},()=>({x:Math.random(),y:Math.random(),r:Math.random()*1.4+.2,a:Math.random()*.75+.15}))}
function drawStars(){const c=el.starfield,ctx=c.getContext("2d");if(!ctx)return;const r=Math.min(devicePixelRatio||1,2);ctx.clearRect(0,0,c.width,c.height);ctx.save();ctx.scale(r,r);const px=delta(state.heading,180)*.35,py=state.pitch*-.7;state.stars.forEach(s=>{const x=((s.x*innerWidth+px)%innerWidth+innerWidth)%innerWidth,y=((s.y*innerHeight+py)%innerHeight+innerHeight)%innerHeight;ctx.globalAlpha=s.a;ctx.fillStyle="#fff";ctx.beginPath();ctx.arc(x,y,s.r,0,Math.PI*2);ctx.fill()});ctx.restore()}
document.addEventListener("click",e=>{const close=e.target.closest("[data-close]");if(close)$(close.dataset.close).classList.remove("open")});
$("startDemo").onclick=demo;$("startAR").onclick=async()=>{await startCamera();await motion()};$("startJourney").onclick=()=>startJourney();$("startAtlas").onclick=openAtlas;$("cameraBtn").onclick=startCamera;$("motionBtn").onclick=motion;$("gpsBtn").onclick=gps;$("atlasBtn").onclick=openAtlas;$("flightBtn").onclick=()=>startJourney();$("finderBtn").onclick=()=>{el.finder.classList.add("open");renderFinder()};$("modeAtlas").onclick=openAtlas;$("modeJourney").onclick=()=>startJourney();$("modeAR").onclick=()=>{el.atlas.classList.remove("open");el.journey.classList.remove("open");setRail("modeAR")};$("lockBtn").onclick=()=>{state.locked=state.selected;state.flightTo=state.selected;toast("Target locked")};$("useJourneyBtn").onclick=()=>startJourney();$("nextStep").onclick=nextStep;$("prevStep").onclick=prevStep;$("backToAR").onclick=()=>el.atlas.classList.remove("open");$("brand").onclick=()=>guide(`${VERSION}. Bright stars use RA/Dec positioning after GPS.`);el.search.oninput=renderFinder;el.atlasSearch.oninput=renderAtlas;document.querySelectorAll(".filter").forEach(f=>f.onclick=()=>{document.querySelectorAll(".filter").forEach(x=>x.classList.remove("active"));f.classList.add("active");state.filter=f.dataset.filter;renderFinder()});document.querySelectorAll(".tab").forEach(t=>t.onclick=()=>{state.tab=t.dataset.tab;renderTab(objectById(state.selected))});window.addEventListener("resize",setupStars);
buildSky();setupStars();renderFinder();renderAtlas();openObject("sun",false);requestAnimationFrame(render);setInterval(()=>{if(el.atlas.classList.contains("open"))renderOrrery()},1500);
